# 关于参与

LiveFlow 是个人项目,主要为自己的使用场景开发。这里说明当前的开放程度,免得你白花时间。

## 当前状态

当前稳定版本为 `v1.1.0`，公共接口遵循语义化版本兼容规则。破坏性变化只进入新的主版本。

## Issue

欢迎。Bug 报告、使用问题和想法都可以提,请走对应的模板。

安全问题请勿公开提交,见 [SECURITY.md](./SECURITY.md)。

## Pull Request

**目前一般不接受功能性 PR。** 这个库的设计边界很窄,大部分「加个功能」的改动都会落在
明确的非目标范围内,或者破坏它对播放器、平台和框架的无关性。与其先写代码再被拒,不如先
开一个 Issue 聊。

以下几类小改动例外,可以直接提 PR:

- 明确的 bug 修复,且附带能复现的测试;
- 文档错误、错别字、失效链接;
- 类型定义错误。

提交前请本地跑通 `pnpm check`(它聚合了格式检查、类型检查、lint、测试与构建)。改动
`src/` 的话需要重新 `pnpm build`,并把 `dist/` 的变化一并提交 —— 本项目不发布到 npm,
通过 Git 安装,因此构建产物随仓库分发。

## 设计边界

无论以什么形式参与,下面几条是不会改变的:

- 核心零运行时依赖;
- 核心不感知具体播放器、解码器、直播平台或前端框架;
- 所有长期运行的数据结构必须可证明有界,过载时丢弃而不是扩容;
- 不用 `innerHTML` 渲染上游数据,不接受任意 HTML;
- 不把失败静默降级成看起来正常的结果;
- 示例与测试只使用合成数据。
