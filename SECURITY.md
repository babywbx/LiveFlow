# 安全政策

## 支持范围

本项目尚未发布 `v1.0.0`，仅对 `main` 与最新的 tag 提供安全修复。

## 报告漏洞

请勿通过公开 Issue 报告安全问题。请使用 GitHub 的 Security Advisory 功能私下提交：

<https://github.com/babywbx/LiveFlow/security/advisories/new>

报告时请尽量包含复现步骤、受影响的入口和预期影响范围。

## 本项目的安全边界

LiveFlow 是纯浏览器侧库，不处理凭据、不发起自定义网络请求、不解析媒体协议。与安全相关的
主要面是**上游消息的渲染**：

- 上游文本只通过 `textContent` 或等价安全 API 输出，不接受任意 HTML；
- 图片与资源只通过调用方提供的解析器加载，库本身不构造 URL；
- 所有队列、集合与字符串长度都有上限，用于抵御高频输入导致的资源耗尽；
- 错误信息不包含完整媒体签名 URL、Cookie 或 token。

如果发现某条上游数据可以绕过上述任意一条，即视为安全问题。

## 凭据

本仓库不发布到 npm，不存在发布凭据，工作流中也不应出现任何密钥。如果你在仓库或构建产物中
发现凭据，请按上述流程报告。
