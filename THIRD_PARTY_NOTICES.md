# Third-Party Notices

The distributed `@babywbx/liveflow` package has no runtime dependencies and does not bundle a
player runtime.

The repository example uses ArtPlayer 5.4.0 as a development-only demonstration dependency.
ArtPlayer is licensed under the MIT License and is not included in the LiveFlow core bundles.

Development tools and their licenses are recorded by `pnpm-lock.yaml`; they are not part of the
distributed runtime package.
